<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyTube" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/youtube</item>
        <item name="actionBarTheme">@color/white</item>
    </style>

    <style name="Theme.MyTube" parent="Base.Theme.MyTube" />
</resources>